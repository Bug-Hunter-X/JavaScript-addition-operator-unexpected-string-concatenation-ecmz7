function add(a, b) {
  return a + b;
}

console.log(add(1, '2')); // Output: "12"

//In JavaScript, the + operator will perform string concatenation if one of the operands is a string.  This can lead to unexpected results when working with numbers.
